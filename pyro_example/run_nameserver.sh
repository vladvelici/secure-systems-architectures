#!/bin/bash

# Run this script in a new terminal window/tab and leave it open. Make sure you
# don't get any errors.

# For Windows: you cannot run .sh scripts, simply type or copy/paste one of the
# commands below (all 3 do the same thing, it just depends on how python 3 and
# pyro4 are installed)

# the three commands below do the same thing. If one does not work, try another.
# Make sure you have pyro4 installed.

python3 -m Pyro4.naming
# python -m Pyro4.naming
# pyro4-ns
